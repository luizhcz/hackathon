# Domain Docs

Before exploring, read `CONTEXT.md` and the relevant ADRs under `docs/adr/`.

This is a single-context repository:

```
/
├── CONTEXT.md
├── docs/adr/
└── src/
```

Use the glossary vocabulary in issues, proposals, hypotheses and test names. Avoid synonyms explicitly rejected by `CONTEXT.md`.

If work contradicts an existing ADR, surface the conflict explicitly instead of silently overriding the decision.
